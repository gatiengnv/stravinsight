{% extends 'base.html.twig' %}

{% block title %}Activities{% endblock %}

{% block body %}
    <div class="container text-center my-5">
        <img src="{{ asset('img/error.png') }}" alt="Error Image" class="img-fluid mx-auto d-block"
             style="max-width: 300px;">

        <h3 class="mt-3 text-danger">An error occurred</h3>
        <p class="mb-4">Unable to access this activity. This activity may not exist or you may not have permission to
            access it.</p>

        <div class="mt-4">
            <a href="{{ path('app_dashboard') }}" class="btn btn-primary">
                Back to home
            </a>
        </div>
    </div>
{% endblock %}
